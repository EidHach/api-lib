{
  "name": "api-lib",
  "version": "0.0.2",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "compile": "rm -rf dist/lib && tsc && tsc --build tsconfig.es5.json"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "axios": "^1.6.1"
  }
}
